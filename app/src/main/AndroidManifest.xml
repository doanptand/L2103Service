<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ddona.service">

    <permission
        android:name="com.ddona.service.LOGIN_PERMISSION"
        android:protectionLevel="signatureOrSystem"/>

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.L2103Service">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.MusicService"
            android:enabled="true"
            android:exported="true" />

        <receiver
            android:name=".receiver.MyFirstBroadcastReceiver"
            android:enabled="true"
            android:permission="com.ddona.service.LOGIN_PERMISSION"
            android:exported="true">
            <!--            <intent-filter-->
            <!--                android:priority="999">-->
            <!--                            <action android:name="android.intent.action.AIRPLANE_MODE" />-->
            <!--                            <action android:name="android.intent.action.SCREEN_OFF" />-->
            <!--                            <action android:name="com.doan.dep.trai" />-->
            <!--                        </intent-filter>-->
        </receiver>
    </application>

</manifest>